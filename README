rfetch is a ruby gem to manage project-sets for different repositories

create a project-set as rake-file:

	require 'rfetch'
	container_1 = Container.new(SvnProvider.new("<url>"))
	container_1.projects << Project.new("<name>")
	...
	container_2 = Container.new(SvnProvider.new("<url>", "<revision>"))
	container_2.projects << Project.new("<name>", "<localname>")
	...
	set = ProjectSet.new("<root-folder>") 
	set.containers << container_1
	set.containers << container_2
	...
	RFetch2Rake.new(set)
	
pull the project-set: 

	rake pull

convert eclipse psf-file to rake-file: 

	rfetch psf2rake -f <psf-file>
	
	
more to come...